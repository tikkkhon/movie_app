/*import React from 'react';
import './FilmPage.css';

class FilmPage extends React.Component {
    componentDidMount() {
        const { location, history } = this.props;
    }
    render(){
        const {location} = this.props;
        return (
        <span className="filmpage">
        <h3 className="film_title"></h3>{location.state.title}</h3>,
        <h5 className="film_year"></h5>{location.state.year}</h5>,
        <p className="film_description"></p>{location.state.description}</p>,
        {location.state.genres}
        </span>
        );
    }
}


export default FilmPage;*/